name: "setup-node"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: "18.x"
    - uses: pnpm/action-setup@v2
      with:
        version: "8.x"
    - run: pnpm install
      shell: bash
